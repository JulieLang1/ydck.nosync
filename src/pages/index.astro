---
import BaseLayout from "../layouts/BaseLayout.astro";
import Card from "../components/Card.astro";
---

<BaseLayout>
  <!-- HERO -->
  <header>
    <section class="hero">
      <a href="#" class="hero-cta">Join the network</a>
      <a href="/">
        <img src="/logo.png" alt="Young Creatives Denmark logo" class="logo" />
      </a>
      <div class="container hero__inner">
        <div class="hero__content">
          <img src="/Logo2.png" alt="Young Creatives Denmark logo" class="logo2" />
          <p><strong>Building the bridge between young creatives and the industry</strong></p>
        </div>
      </div>
    </section>
  </header>
  <!-- CARDS -->
  <section class="section">
    <div class="container">
      <h2 class="section__title">Mød YCDK bestyrelsen.</h2>
      <p>Lær temaet bag YCDK at kende!</p>

      <div class="cards">
        <Card title="Rebecca Laursen" img="rebecca.png" text="Rebecca er en del af YCDK, fordi branchen hurtigt kan blive en lukket klub, og unge kreative fortjener reelle muligheder for at få foden indenfor branchen." />
        <Card title="Sanne Kragh" img="sanne.png" text="Sanne har ikke taget den klassiske vej ind i branchen og har selv stået udenfor og kigget ind. YCDk hjalp hende med at åbne døren. Derfor vil hun gerne hjælpe andre nye." />
        <Card title="Sarah Sabir" img="sarah.png" text="Sarah vil gerne bidrage med sin viden om den kreative branche og udbrede budskabet til unge om, at kreativt arbejde ikke kun behøver at være et 9-16 job på et bureau." />
        <Card title="Jake Brook" img="jakejake 1.png" text="Jake er en del af YCDK, fordi de hjalp ham med at overkomme hans socialangst med networking. Den oplevelse vil han gerne give videre og lære fra sig, så meget han kan." />
      </div>
    </div>
  </section>

  <!-- den store lyseblå blok) -->
  <section class="section">
    <div class="cta">
      <h2>Bliv en del af vores netværk!<br />Kom med til næste event!</h2>
      <a class="btn" href="#">Følg med her!</a>
    </div>
  </section>
</BaseLayout>

<!-- CONTENT BLOCKS -->
<!-- CONTENT BLOCKS -->
<section class="section content-blocks">
  <div class="container">
    <!-- Feature 1 -->
    <div class="feature">
      <div class="feature__media">
        <img src="/løve.png" alt="..." />
      </div>

      <div class="feature__text">
        <h3>SPEEDDATE EN STRANDLØVE- CANNES</h3>
        <p>Så er det lige om snart du kan speeddate de erfarne kreative i Cannes! Eventet starter kl 15 på baren Brown Sugar beliggende på 17 Rue Fréres Pradignac, Cannes. Vi glæder os til at se jer!</p>
        <a class="btn btn--small" href="https://www.youtube.com/shorts/w_M8lHPRylo">Læs mere her!</a>
      </div>
    </div>

    <!-- Feature 2 (reverse) -->
    <div class="feature feature--reverse">
      <div class="feature__media">
        <img src="/ansigt.png" alt="..." />
      </div>

      <div class="feature__text">
        <h3>ANSIGT TIL ANSIGT</h3>
        <p>Her kan du møde de kreative ansigt til ansigt og få en snak om mulighederne i branchen. Og måske finde din næste samarbejdspartner!</p>
        <a class="btn btn--small" href="https://www.youtube.com/shorts/KtfRUhRoAHw">Læs mere her!</a>
      </div>
    </div>
  </div>
</section>

<!-- FOOTER -->
<section class="footerband">
  <div class="footerband__inner">
    <!-- Venstre blok -->
    <div class="footer-left">
      <img src="/Logo2.png" alt="logo" />
      <p class="footer-description">Et netværk drevet af passion, kreativitet og nye forbindelser.</p>
    </div>

    <!-- Højre blok -->
    <div class="footer-right">
      <div class="footer-col">
        <h4>Socials</h4>
        <a href="https://www.facebook.com/youngcreativesdk">Facebook</a>
        <a href="https://www.instagram.com/youngcreativesdenmark/">Instagram</a>
      </div>

      <div class="footer-col">
        <h4>Telefon</h4>
        <p>60 91 43 26</p>
      </div>

      <div class="footer-col">
        <h4>Adresse</h4>
        <p>
          Jacob Cameron Brook,<br />
          Landskronagade 27, st.<br />
          2100 København Ø
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .container {
    max-width: 980px;
    margin: 0 auto;
    padding: 0 24px;
  }

  /* HERO */
  .hero {
    background: linear-gradient(135deg, var(--blue-dark), var(--blue-light));
    min-height: 420px;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
  }
  .hero__inner {
    position: relative;
    padding: 56px 0 64px;
    width: 100%;
    display: flex;
    justify-content: center;
  }
  .logo {
    position: absolute;
    top: 24px;
    left: 24px;
    width: 84px;
    height: auto;
  }
  .hero__content {
    text-align: center;
    color: var(--white);
    padding-top: 40px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
  }
  .hero h1 {
    margin: 0;
    font-size: 44px;
    line-height: 0.95;
    letter-spacing: 0.02em;
  }
  .hero p {
    margin-top: 14px;
    opacity: 0.95;
  }

  /* GENERELLE SEKTIONER */
  .section {
    padding: 56px 0;
    background: var(--white);
  }
  .section__title {
    margin: 0 0 18px;
    font-size: 22px;
  }

  /* CARDS = 3 kolonner */
  .cards {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }

  /* CTA = stor lyseblå blok */
  .cta {
    background: #82c2f0;
    border-radius: 18px;
    padding: 44px 28px;
    text-align: center;
    color: var(--white);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
    font-family: "Montserrat";
  }
  .cta h2 {
    margin: 0 0 16px;
    font-size: 28px;
    line-height: 1.1;
    font-family: "Montserrat";
  }

  /* BUTTON */
  .btn {
    display: inline-block;
    background: #f1a3c3;
    color: var(--white);
    text-decoration: none;
    font-weight: 700;
    padding: 10px 18px;
    border-radius: 999px;
  }
  .btn--small {
    padding: 9px 14px;
    font-size: 14px;
    font-family: "Montserrat";
  }
  .btn:hover {
    background: #ffe4f2;
  }
  /* FEATURE BLOCKS */
  .feature {
    display: flex;
    align-items: center;
    gap: 48px;
    margin-bottom: 64px; /* afstand mellem de to blokke */
  }

  .feature__media {
    flex: auto; /* billedets bredde (justér) */
    aspect-ratio: 4 / 5; /* holder boksen ens */
    border-radius: 18px;
    overflow: hidden; /* så billedet klippes pænt i runde hjørner */
    background: #ddd;
  }

  .feature__media img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* fylder boksen pænt */
    display: block;
  }

  /* Hvis du har en reverse-klasse */
  .feature--reverse {
    flex-direction: row-reverse;
  }

  .feature__text h3 {
    margin: 0 0 10px;
    font-size: 32px; /* prøv 28–40px */
    line-height: 1.2;
    font-weight: 700;
  }

  .feature__text p {
    margin: 0 0 14px;
    max-width: 42ch;
    font-size: 18px; /* prøv 17–20px */
    line-height: 1.6;
  }
  @media (max-width: 900px) {
    .feature {
      flex-direction: column;
      align-items: stretch;
    }

    .feature__media {
      flex: none;
      width: 100%;
      aspect-ratio: 16 / 10;
    }
  }
  @media (min-width: 900px) {
    .feature_text h3 {
      font-size: 36px;
    }

    .feature_text p {
      font-size: 19px;
    }
  }

  /* Kun content blocks (de 2 store billeder) */
  .content-blocks .feature {
    display: flex;
    align-items: center;
    gap: 48px;
    margin-bottom: 64px;
  }

  /* Fast “ramme” så layoutet bliver som i prototype */
  .content-blocks .feature__media {
    flex: 0 0 420px;
    aspect-ratio: 3 / 4; /* portrait format */
    border-radius: 18px;
    overflow: hidden; /* ingen grå baggrund */
  }

  /* Hele billedet synligt – ingen grå baggrund */
  .content-blocks .feature__media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  /* Reverse layout */
  .content-blocks .feature--reverse {
    flex-direction: row-reverse;
  }

  /* Mobil */
  @media (max-width: 900px) {
    .content-blocks .feature {
      flex-direction: column;
      align-items: stretch;
    }

    .content-blocks .feature__media {
      width: 100%;
      aspect-ratio: 3 / 4; /* højere end bred */
    }
  }

  /* FOOTERBAND = pink bund */
  .footerband {
    background: linear-gradient(135deg, #f1a3c3, #ff5bbf);
    color: white;
    padding: 40px 20px;
  }
  .footerband__inner {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 24px;
  }
  .footerlinks {
    display: flex;
    gap: 18px;
    font-family: "Montserrat";
  }
  .footerlinks a {
    color: var(--white);
    text-decoration: none;
    opacity: 0.95;
    font-family: "Montserrat";
  }

  /* RESPONSIV */
  @media (max-width: 820px) {
    .cards {
      grid-template-columns: 1fr;
    }
    .feature {
      grid-template-columns: 1fr;
    }
    .feature--reverse .ph,
    .feature--reverse .feature__text {
      order: initial;
    }
  }
</style>
